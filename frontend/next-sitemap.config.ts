import type { IConfig } from "next-sitemap";

const config: IConfig = {
  siteUrl: "https://morningnewsbd-xtqk.vercel.app/",
  generateRobotsTxt: true,
  sitemapSize: 7000,
};

export default config;
